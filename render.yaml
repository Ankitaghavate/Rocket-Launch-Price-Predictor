services:
  - type: web
    name: your-app-name
    env: python
    buildCommand: pip install --only-binary :all: -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PIP_ONLY_BINARY
        value: :all:
